pragma solidity ^0.5.11;

// import { DelegationManager as IDelegationManager } from "matic-protocol/contracts/staking/DelegationManager.sol";
// import { ValidatorContract as IValidatorContract } from "matic-protocol/contracts/staking/Validator.sol";

contract DelegatorContract {
  // IDelegationManager delegationManager;
  // uint256 validatorId;
  // // IValidatorContract validatorContract;

  // uint256 delegatorId;

  // constructor(address _delegationManager, uint256 _validatorId) public {
  //   delegationManager = IDelegationManager(_delegationManager);
  //   validatorId = _validatorId;
  //   // validatorContract = IValidatorContract(_validatorContract);
  // }

  // function stake(uint256 amount) public /* onlyMaticStaker */ {
  //   if (delegatorId == 0) {
  //     delegatorId = delegationManager.NFTCounter();
  //     delegationManager.stake(amount);
  //   } else {
  //     delegationManager.reStake(delegatorId, amount, true /* stakeRewards */);
  //   }
  //   delegationManager.bond(delegatorId, validatorId);
  // }

  // function unStake(uint256 amount) public /* onlyMaticStaker */ {

  // }
}
